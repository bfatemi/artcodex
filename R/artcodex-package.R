#' artcodex: An R client for local Codex app-server sessions
#'
#' `artcodex` provides a small synchronous R interface to the local Codex
#' app-server. Start with [codex_run()] for a self-contained call. Use
#' [codex_client_start()] and [codex_thread_start()] when a pipeline needs to
#' reuse a process or retain conversation state.
#'
#' @keywords internal
"_PACKAGE"
