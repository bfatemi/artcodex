% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/thread.R
\name{codex_run}
\alias{codex_run}
\title{Run Codex from R}
\usage{
codex_run(
  prompt,
  thread = NULL,
  client = NULL,
  images = NULL,
  cwd = if (is.null(thread)) getwd() else NULL,
  model = NULL,
  sandbox = NULL,
  approval_policy = NULL,
  ephemeral = NULL,
  output_schema = NULL,
  config = NULL,
  base_instructions = NULL,
  developer_instructions = NULL,
  thread_extra = NULL,
  turn_extra = NULL,
  timeout = 300,
  client_timeout = 30,
  progress = FALSE,
  on_event = NULL,
  request_handler = NULL,
  stop_on_error = FALSE,
  command = NULL,
  client_args = "app-server",
  client_env = NULL
)
}
\arguments{
\item{prompt}{One text prompt. For compatibility, an \code{artcodex_thread} may be
passed first and the prompt second as \code{codex_run(thread, prompt)}.}

\item{thread}{Optional existing \code{artcodex_thread}.}

\item{client}{Optional existing \code{artcodex_client}. When supplied without a
thread, a new thread is created on this client.}

\item{images}{Optional character vector of local image paths.}

\item{cwd}{Working directory for a newly created thread. For an existing
thread, \code{NULL} keeps its current working directory.}

\item{model}{Optional model override.}

\item{sandbox}{Sandbox mode for a newly created thread.
Automatic threads default to \code{"read-only"}. This cannot be changed on an
existing thread through \code{codex_run()}.}

\item{approval_policy}{Optional approval policy for the thread or turn.
Automatic threads default to \code{"never"}; existing threads retain their
policy when this is \code{NULL}.}

\item{ephemeral}{Whether an automatically created thread should be
non-persistent. Automatic threads default to \code{TRUE}.}

\item{output_schema}{Optional JSON Schema list for structured output. Parsed
JSON is returned in \code{result$output}.}

\item{config}{Optional Codex configuration overrides for a new thread.}

\item{base_instructions}{Optional replacement base instructions for a new
thread.}

\item{developer_instructions}{Optional additional developer instructions for
a new thread.}

\item{thread_extra}{Optional additional fields for \code{thread/start}.}

\item{turn_extra}{Optional additional fields for \code{turn/start}.}

\item{timeout}{Maximum number of seconds for the turn.}

\item{client_timeout}{Maximum number of seconds for client initialization.}

\item{progress}{If \code{TRUE}, print event names while the turn runs.}

\item{on_event}{Optional function called for each streamed event.}

\item{request_handler}{Optional function that answers server-initiated
requests. Unhandled requests fail closed.}

\item{stop_on_error}{If \code{TRUE}, throw an \code{artcodex_turn_error} when the result
is unsuccessful. The default returns a serializable failure result.}

\item{command}{Optional path to the Codex executable for an automatic client.}

\item{client_args}{Command-line arguments for an automatic client. This is
primarily useful for testing or custom app-server launchers.}

\item{client_env}{Optional process environment for an automatic client.}
}
\value{
An \code{artcodex_result}. Use \code{codex_succeeded()} to test success.
}
\description{
With only a prompt, \code{codex_run()} starts and cleans up a local app-server and
thread automatically. Pass an existing thread to retain conversation state
across calls.
}
\examples{
\dontrun{
result <- codex_run("Summarize the R files in this project.")
result$final_response

client <- codex_client_start()
thread <- codex_thread_start(client)
first <- codex_run("Inspect the package.", thread = thread)
second <- codex_run("Now suggest three improvements.", thread = thread)
codex_client_stop(client)
}
}
